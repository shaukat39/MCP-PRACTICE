import {
  __commonJS,
  __require
} from "./chunk-FFYIGW52.mjs";

// src/example.ts
import { chatCompletionStream } from "@huggingface/inference";
var require_example = __commonJS({
  "src/example.ts"(exports, module) {
    async function main() {
      const endpointUrl = `http://localhost:9999/v1/chat/completions`;
      for await (const chunk of chatCompletionStream({
        endpointUrl,
        model: "",
        messages: [{ role: "user", content: "What are the top 5 trending models on Hugging Face?" }]
      })) {
        console.log(chunk.choices[0]?.delta.content);
      }
    }
    if (__require.main === module) {
      main();
    }
  }
});
export default require_example();
